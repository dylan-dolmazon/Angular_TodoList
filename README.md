# Angular_TodoList

## Installation

- Mettre à jour NodeJs:
  - `npm cache clean -f`
  - `npm install -g n`
  - `sudo n stable`
- Installer AngularJs
  - `npm i -g @angular/cli`
- Nouveau projet
  - `ng new project_name`
- Creer un composant
  - `cd project_name`
  - `g generate c composant_name`
- Lancer le server
  - `npm start`
   

